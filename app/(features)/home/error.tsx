"use client";

import { ErrorPage } from "@shared/components";

export default function Error() {
  return (
    <ErrorPage message="Something went really wrong when loading the home screen. Maybe try again later?" />
  );
}
